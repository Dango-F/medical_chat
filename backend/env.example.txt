# Application
APP_NAME="Medical KG QA System"
APP_ENV=development
DEBUG=true
SECRET_KEY=medical-kg-secret-key-2024

# Server (后端服务端口)
HOST=0.0.0.0
PORT=8000

# API Keys (配置其中一个即可，不配置则使用知识图谱直接回答)
OPENAI_API_KEY=
GEMINI_API_KEY=
SILICONFLOW_API_KEY=your-siliconflow-api-key

# LLM Provider: "siliconflow", "gemini", "openai", or "mock"
# siliconflow - 使用硅基流动的 DeepSeek 模型（推荐）
# mock 模式会直接使用知识图谱数据回答，不需要 API Key
LLM_PROVIDER=siliconflow

# SiliconFlow 配置（硅基流动）
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
# 可选模型: deepseek-ai/DeepSeek-V3 或 deepseek-ai/DeepSeek-R1（深度思考）
SILICONFLOW_MODEL=deepseek-ai/DeepSeek-V3

# Neo4j Knowledge Graph (已配置 - 确保 Neo4j 正在运行)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=medical123

# Qdrant Vector Database
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=medical_docs

# PostgreSQL (Metadata & Audit)
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/medical_kg

# Redis (Cache)
REDIS_URL=redis://localhost:6379

# CORS
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# Logging
LOG_LEVEL=INFO

# ============================================
# RENAME THIS FILE TO .env BEFORE RUNNING
# ============================================

